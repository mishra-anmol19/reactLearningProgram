{
  "presets":[
    [
    "@bables/preset-env",
      {
        "targets": {
          "esmodules": true
        }
      }
    ],

    [
      "@bables/preset-react",
      {
        "runtime": "automatic"
      }
    ]
  ]
}